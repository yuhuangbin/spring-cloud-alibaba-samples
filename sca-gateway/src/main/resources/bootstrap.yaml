spring:
  application:
    name: sca-gateway

  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: 127.0.0.1:8848
      config:
        shared-configs:
          - data-id: gateway.properties

